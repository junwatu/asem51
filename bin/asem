#! /usr/bin/env node

'use strict';

const argv = require('minimist')(process.argv.slice(2));
const ASEM51 = require('../lib/asem51');

ASEM51.asyncAsem(argv)
.then(data => console.log(data))
.catch(err => console.log(err));